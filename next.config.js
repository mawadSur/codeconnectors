/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    assetPrefix: './',
    images: {
        unoptimized: true,
    },
// This is a random comment

}

module.exports = nextConfig
